---
sidebar_position: 8
---
# Một số từ khóa trong Java

Dưới đây là danh sách **keyword** (từ khóa) trong Java.  
Java có khoảng **50 từ khóa**, và bạn *không được phép* dùng chúng làm tên biến, tên hàm hoặc tên class.  
Lưu ý: `true`, `false`, `null` **không phải là keyword**.

---

## Nội dung

1. [Danh sách từ khóa & ý nghĩa](#--danh-sách-từ-khóa--ý-nghĩa)  
2. [Ghi nhớ nhanh](#2--ghi-nhớ-nhanh)  

---

## 🔑 Danh sách từ khóa & ý nghĩa

| Keyword | Ý nghĩa |
|--------|---------|
| `abstract` | Khai báo lớp/phương thức trừu tượng (không có implementation) |
| `assert` | Kiểm tra điều kiện (thường dùng trong Unit Test) |
| `boolean` | Kiểu logic (true/false) |
| `break` | Thoát khỏi vòng lặp hoặc `switch` |
| `byte` | Kiểu số nguyên 8-bit |
| `case` | Một nhánh trong `switch` |
| `catch` | Bắt exception trong khối `try-catch` |
| `char` | Kiểu ký tự Unicode 16-bit |
| `class` | Khai báo lớp |
| `const` | Không sử dụng trong Java |
| `continue` | Bỏ qua vòng lặp hiện tại, chạy vòng tiếp theo |
| `default` | Nhánh mặc định trong `switch` |
| `do` | Vòng lặp `do-while` |
| `double` | Kiểu số thực 64-bit |
| `else` | Nhánh điều kiện ngược lại của `if` |
| `enum` | Kiểu liệt kê |
| `extends` | Kế thừa lớp cha |
| `final` | Không cho sửa đổi biến/phương thức/lớp |
| `finally` | Luôn chạy dù có exception hay không |
| `float` | Kiểu số thực 32-bit |
| `for` | Vòng lặp `for` |
| `goto` | Không sử dụng trong Java |
| `if` | Câu điều kiện |
| `implements` | Implement interface |
| `import` | Nhập các lớp từ package |
| `instanceof` | Kiểm tra object có thuộc kiểu nào |
| `int` | Kiểu số nguyên 32-bit |
| `interface` | Khai báo interface |
| `long` | Kiểu số nguyên 64-bit |
| `native` | Dùng code từ ngôn ngữ khác (JNI) |
| `new` | Tạo object mới |
| `package` | Khai báo package chứa class |
| `private` | Chỉ truy cập trong class |
| `protected` | Truy cập trong class, subclass và cùng package |
| `public` | Truy cập từ mọi nơi |
| `return` | Trả về giá trị trong phương thức |
| `short` | Kiểu số nguyên 16-bit |
| `static` | Thành phần thuộc class, không cần tạo object |
| `super` | Gọi đến lớp cha |
| `switch` | Cấu trúc rẽ nhánh |
| `synchronized` | Đồng bộ hóa trong đa luồng |
| `this` | Tham chiếu đến object hiện tại |
| `throw` | Ném exception |
| `throws` | Khai báo ngoại lệ có thể xảy ra |
| `transient` | Không lưu biến khi serialize |
| `try` | Khối thử để bắt lỗi |
| `void` | Phương thức không trả về |
| `volatile` | Biến có thể thay đổi giữa các thread |
| `while` | Vòng lặp `while` |

---

## 🎯 Ghi nhớ nhanh  
- Không đặt tên biến trùng keyword  
- Phần lớn keyword liên quan đến:  
  ✔ cấu trúc điều khiển  
  ✔ định nghĩa lớp  
  ✔ xử lý lỗi  
  ✔ đa luồng  
  ✔ kiểu dữ liệu  

---
